#include <2104.Sum_of_Subarray_Ranges.hpp>
#include <iostream>

int main() {
    using namespace std;

    lc::Solution sol{};
    std::vector<int> nums{1, 2, 3};

    cout << sol(nums);
    cout << endl;
}